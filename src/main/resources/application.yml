# Config Server에서 설정 로드 (Config Server 미실행 시 optional로 기동 가능)
spring:
  application:
    name: a-gateway
  config:
    import: optional:configserver:http://localhost:8888
  # 로컬 기본값 (Config Server 없을 때 사용). Config Server 사용 시 라우트는 config-repo에서 오며 여기는 무시됨
#  cloud:
#    gateway:
#      routes: []
